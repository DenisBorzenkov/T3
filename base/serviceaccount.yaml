apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-server
  namespace: default
  labels:
    app: api-server
# The app does not talk to the Kubernetes API, so it gets a dedicated SA
# and we never mount its token (defence in depth — a leaked token is useless).
automountServiceAccountToken: false
